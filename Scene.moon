-- Scene [Class]
--
-- This is the class which all Scenes inherits. It's in very early stage, so
-- it's acting actually like a interface or something alike. There's no need
-- for more comments yet.
class Scene
	new: =>
		return
	
	update: =>
		return
		
	dispose: =>
		return
